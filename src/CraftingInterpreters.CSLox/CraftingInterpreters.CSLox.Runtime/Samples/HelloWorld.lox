var myName = "Hello World!";

// Use the new print keyword to print myName to the console
print myName;

/* Following code is cool but it has to be stopped 
  because it is too cool for this world */
var my_result = 15 + 5;

fun Add(a, b) {
	return a + b;
}

var data = "This is amazing";